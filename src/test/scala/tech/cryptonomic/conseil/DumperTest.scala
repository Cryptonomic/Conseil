package tech.cryptonomic.conseil

import org.scalatest._

class DumperTest extends FlatSpec with Matchers {
  /*it should "return the correct number of blocks given a valid interval" in {
    val blocks = Dumper.takeADump(1,5)
    blocks.size should be (5)
  }

  it should "return an empty list when given an invalid interval" in {
    val blocks = Dumper.takeADump(6,5)
    blocks.size should be (0)
  }

  it should "return a singleton list when given a unit interval" in {
    val blocks = Dumper.takeADump(5,5)
    blocks.size should be (1)
  }*/

}
